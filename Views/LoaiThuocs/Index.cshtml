@model IEnumerable<BTL.Models.LoaiThuoc>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenLt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaLt">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaLt">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaLt">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
