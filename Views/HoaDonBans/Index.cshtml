@model IEnumerable<BTL.Models.HoaDonBan>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MaTk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayBan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaTkNavigation.MaTk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayBan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaHdb">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaHdb">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaHdb">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
